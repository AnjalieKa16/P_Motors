import React from 'react';
import { Typography } from '@mui/material';

const Dashboard = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>Dashboard Overview</Typography>
      <p>Show sales stats, orders, etc.</p>
    </div>
  );
};

export default Dashboard;
